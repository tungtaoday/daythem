services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: daythem
      POSTGRES_USER: daythem
      POSTGRES_PASSWORD: daythem
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
